# yaml-language-server: $schema=../../../environment.schema.json
---
sources:
  my-tool:
    https_url_template: https://sub.domain.com/test-tool/{platform}-{arch}-{version}.zip
    archive_path_template: my-tool{exe}
