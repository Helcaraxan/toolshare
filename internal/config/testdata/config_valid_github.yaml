sources:
  test-tool-plain:
    type: github
    github:
      slug: repo/tool-plain
      release_asset_template: test-tool_{os}-{arch}-{version}{exe}
  test-tool-archive:
    type: github
    github:
      slug: repo/tool-archive
      release_asset_template: test-tool_{os}-{arch}-{version}.tar.gz
      archive_path_template: bin/test-tool{exe}
  test-tool-enterprise:
    type: github
    github:
      base_url: https://git.my-enterprise.io
      slug: repo/tool-enterprise
      release_asset_template: test-tool_{os}-{arch}-{version}{exe}
